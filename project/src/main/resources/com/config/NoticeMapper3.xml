<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.mapper.NoticeMapper3">

    <select id="selectNoticeList" resultType="NoticeDto">
        SELECT post_Id, title, content , good, view_cnt, member_id 
        FROM post_test
        ORDER BY 1 DESC
    </select>
    
    <select id="selectNoticePage" resultType="NoticeDto" parameterType="PageDTO">
        SELECT post_Id, title, content , good, view_cnt, member_id 
        FROM (SELECT rownum rm, post_id, title, content, good, view_cnt, member_id FROM post_test ORDER BY post_id DESC)
        WHERE rm between #{start} AND #{end}
    </select>
    
    
    <select id="selectSearchNoticePage" resultType="NoticeDto" parameterType="PageDTO">
       	
    </select>
    <!-- 
    <select id="selectSearchNoticePage" resultType="NoticeDto" parameterType="PageDTO">
        SELECT post_Id, title, content , good, view_cnt, member_id 
        FROM (SELECT rownum rm, post_id, title, content, good, view_cnt, member_id FROM post_test ORDER BY post_id DESC)
        WHERE rm between #{start} AND #{end} AND title LIKE '%'||#{keyword}||'%'
    </select>
     -->
    
    <update id="updateViewCnt" parameterType="long">
        update post_test
        set view_cnt = view_cnt + 1
        where post_id = #{postId}
</update>
    
    <select id="noticeTotalCnt" resultType="int">
        SELECT count(*) FROM post_test
    </select>
    
    <select id="noticeSearchTotalCnt" resultType="int" parameterType="string">
        SELECT count(*) FROM post_test
        WHERE title LIKE '%'||#{keyword}||'%'
    </select>
    
	<insert id="insertNotice" parameterType="NoticeDto">
		INSERT INTO post_test (post_id, title, write_time, category_id,content, member_id) 
		VALUES(seq_post.nextval, #{title}, sysdate, 1, #{content}, 1234)
	</insert>
	
	<update id="updateNotice" parameterType="NoticeDto">
		UPDATE post_test
			SET title = #{title},
    			content = #{content}
		WHERE post_id = #{postId}
	</update>
	
	<delete id="deleteNotice" parameterType="long">
		delete  from post_test
		where post_id = #{postId}
	</delete>
	
	<select id="selectByPostId" resultType="NoticeDto" parameterType="long">
        SELECT post_Id, title, content , good, view_cnt, member_id 
        FROM post_test
        WHERE post_Id = #{postId}
    </select>
  
	
	
</mapper>