<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.mapper.NoticeMapper2">

    <select id="selectNoticeList" resultType="NoticeDto">
        SELECT post_Id, title, content , good, view_cnt, member_id 
        FROM post_test
        ORDER BY 1 DESC
    </select>
    
	<insert id="insertNotice" parameterType="NoticeDto">
		insert into post_test (post_id, title, write_time, category_id,content, member_id) 
		values(seq_post.nextval, #{title}, sysdate, 1, #{content}, 1234)
	</insert>
	
	<update id="updateNotice" parameterType="NoticeDto">
		update post_test
			set title = #{title},
    			content = #{content}
		where post_id = #{postId}
	</update>
	
	<delete id="deleteNotice" parameterType="long">
		delete  from post_test
		where post_id = #{postId}
	</delete>
	
	<select id="selectByPostId" resultType="NoticeDto" parameterType="int">
        SELECT post_Id, title, content , good, view_cnt, member_id 
        FROM post_test
        WHERE post_Id = #{postId}
    </select>
	
	
</mapper>