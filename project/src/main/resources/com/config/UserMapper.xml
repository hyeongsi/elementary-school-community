<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.example.project.dao.UserDAO">
		
		<!-- 로그인한 회원 정보 -->
   		<select id="getUserById" parameterType="UserDTO" resultType="UserDTO">
  			SELECT *
  			FROM MEMBER_DATA
  			WHERE ID=#{id}
  		</select>
  		
  		<!-- 회원가입 -->
  		<insert id="insertUser" parameterType="UserDTO">
  			INSERT INTO MEMBER_DATA(ID, PWD, NAME, EMAIL, EDUCATION, SCHOOL, EDUCATION_CODE, SCHOOL_CODE)
					VALUES(#{id}, #{pwd}, #{name}, #{email}, #{education}, #{school}, #{education_code}, #{school_code})
  		</insert>
  		
  		<!-- 회원정보 수정 -->
  		<insert id="updateUser" parameterType="UserDTO">
  		 	UPDATE MEMBER_DATA
  		 	SET NAME = #{name},
  		 		EMAIL = #{email},
  		 		EDUCATION = #{education},
  		 		SCHOOL = #{school},
  		 		EDUCATION_CODE = #{education_code},
  		 		SCHOOL_CODE = #{school_code}
  		 	WHERE ID = #{id}
  		</insert>
  		
  		<!-- 회원탈퇴 -->
  		<delete id="deleteUser" parameterType="UserDTO">
  			DELETE
  			FROM MEMBER_DATA
  			WHERE ID = #{id}
  		</delete>
  		
   
  </mapper>